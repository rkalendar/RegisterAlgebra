# RegisterAlgebra
Reference implementation of Register Algebra — an exact integer and rational arithmetic in which every integer N is represented as a pair of integer registers (a, b) under the evaluation map
